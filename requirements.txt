# IMPORTANT: Do NOT install this file before PyTorch.
# peft depends on torch>=1.13.0, so pip will auto-install the latest torch
# wheel (e.g. cu130) which may exceed your NVIDIA driver's CUDA support.
#
# Install PyTorch FIRST with a pinned version, then use constraints:
#
#   pip install --no-cache-dir torch==2.7.0 --index-url https://download.pytorch.org/whl/cu126
#   cat > constraints-cu126.txt <<'EOF'
#   torch==2.7.0
#   EOF
#   pip install -r requirements.txt -c constraints-cu126.txt
#
# https://pytorch.org/get-started/locally/

transformers>=4.51.0,<5
datasets>=2.20.0,<4
accelerate>=0.30.0,<2
peft>=0.11.0,<1
scikit-learn>=1.4.0,<2
openai>=1.30.0,<3
httpx>=0.28.0,<1
numpy>=1.26.0,<3
tqdm>=4.66.0,<5
python-dotenv>=1.0.1,<2