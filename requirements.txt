# IMPORTANT: This file does NOT include torch/torchvision/torchaudio.
# PyTorch must be installed separately according to your CUDA/driver version.
# Install this file first, then install PyTorch:
#
#   pip install -r requirements.txt
#   nvidia-smi   # check your driver
#   pip install torch --index-url https://download.pytorch.org/whl/cu126  # or cpu/cu121/cu124
#
# https://pytorch.org/get-started/locally/

transformers>=4.51.0,<5
datasets>=2.20.0,<4
accelerate>=0.30.0,<2
peft>=0.11.0,<1
scikit-learn>=1.4.0,<2
openai>=1.30.0,<3
matplotlib>=3.8.0,<4
numpy>=1.26.0,<3
tqdm>=4.66.0,<5
python-dotenv>=1.0.1,<2